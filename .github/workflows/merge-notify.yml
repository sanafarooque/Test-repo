name: Notify on merge

on:
  push:
    branches:
      - main
      - master
      - '**'

jobs:
  notify:
  if: github.event.head_commit.message != ''
  runs-on: ubuntu-latest
  steps:
   - name: Send Notification
     run: echo "Merge occurred on branch ${{ github.ref }} by ${{ github.actor }}"
     
