name: Notify Teams on PR Merge

on:
  pull_request:
    types:
      - closed   # Triggers when PR is closed (merged OR just closed)

jobs:
  notify:
    if: github.event.pull_request.merged == true   # Only if actually merged
    runs-on: ubuntu-latest
    steps:
      - name: Send MS Teams notification
        uses: neonidian/teams-notify-build-status@v4
        with:
          webhookUrl: ${{ secrets.MS_TEAMS_WEBHOOK }}
          title: "✅ Pull Request Merged"
          titleBackgroundColor: "green"
          status: "SUCCESS"
          message: |
             **Repository:** ${{ github.repository }}
             **PR:** [#${{ github.event.pull_request.number }}](${{ github.event.pull_request.html_url }}) - ${{ github.event.pull_request.title }}
             **Author:** ${{ github.event.pull_request.user.login }}
             **Merged by:** ${{ github.event.pull_request.merged_by.login }}
#          raw: |
#            {
#              "@type": "MessageCard",
#              "@context": "http://schema.org/extensions",
#              "themeColor": "#cfe2f3",
#              "summary": "Pull Request Merged",
#              "sections": [{
#                "activityTitle": "✅ Pull Request Merged",
#                "activitySubtitle": "#${{ github.event.pull_request.number }} - ${{ github.event.pull_request.title }}",
#                "activityImage": "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png",
#                "facts": [
#                  { "name": "Repository", "value": "${{ github.repository }}" },
#                  { "name": "Merged by", "value": "${{ github.event.pull_request.merged_by.login }}" },
#                  { "name": "Branch", "value": "${{ github.event.pull_request.head.ref }} → ${{ github.event.pull_request.base.ref }}" }
#                ],
#                "markdown": true
#              }]
#            }
            

